import { StyleSheet } from 'react-native';

export const PinnedLocationStyles = StyleSheet.create({ 

    container: {
        flex: 1,
    },

    content: {
        padding: 20,
        paddingBottom: 40,
    },

    locationList: {
        display: 'flex',
        gap: 20
    }
});
